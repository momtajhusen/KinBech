/**
 * Production API host (fallback when EXPO_PUBLIC_API_URL is unset).
 * Live VPS nginx: kinbechapi.codersalpha.com
 * (kinbech-api with hyphen needs DNS+nginx alias for old builds)
 */
export const PRODUCTION_API_URL = 'https://kinbechapi.codersalpha.com';
